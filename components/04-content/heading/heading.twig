{%
  set classes = [
    variant and variant != 'default' ? variant,
  ]
%}

{% set attributes = attributes.addClass(classes) %}

<{{ html_tag|default('h1') }}{{attributes}}>
  {{ content }}
</{{ html_tag|default('h1') }}>
