name: container
group: 00-base
status: experimental

props:
  type: object
  properties:
    modifier_class:
      title: Modifier Class
      type: string
      description: Extra classes for custom styling or variants.
      items:
        type: string
      examples:
        - container--full

slots:
  content:
    title: Content
    type: string
    description: Content slot.
    examples:
     - '<strong>sample html content</strong>'
