name: table

status: experimental
group: content

props:
  type: object
  properties:
    html_tag:
      type: string
      default: table
      enum:
        - table
        - div

slots:
  content:
    title: Content
